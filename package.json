{
  "name": "extendyourperspectives",
  "version": "0.0.1",
  "description": "This starter plugin is a good place to get started when developing for Photoshop. It does not rely on any frameworks or build steps -- hence the name \"Vanilla\".",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Taki",
  "license": "ISC",
  "dependencies": {
    "gulp": "^4.0.2",
    "typescript": "^4.6.4"
  },
  "devDependencies": {
    "gulp-concat": "^2.6.1",
    "jest": "^28.1.0"
  }
}
